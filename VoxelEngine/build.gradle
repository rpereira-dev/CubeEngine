dependencies {
	compile group: 'commons-collections', name: 'commons-collections', version: '3.2'
}

// configure  the source folders
sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
}